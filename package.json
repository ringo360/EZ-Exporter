{
    "name": "ez-exporter",
    "module": "src/main.ts",
    "type": "module",
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    },
    "scripts": {
        "build-win": "bun build src/index.ts --minify --compile --target=bun-windows-x64 --outfile build/run_win",
        "build-linux": "bun build src/index.ts --minify --compile --target=bun-linux-x64 --outfile build/run_linux"
    }
}
